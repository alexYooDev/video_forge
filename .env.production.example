# VideoForge Production Environment Configuration
# Copy this file to .env.production and update with your actual values

# Database Configuration
DB_ROOT_PASSWORD=your-very-secure-root-password-here
DB_NAME=video_forge
DB_USER=video_user
DB_PASSWORD=your-secure-database-password-here

# JWT Configuration (IMPORTANT: Use a strong, unique secret)
JWT_SECRET=your-very-secure-jwt-secret-key-with-at-least-32-characters
JWT_EXPIRES_IN=24h

# Video Processing Configuration
MAX_CONCURRENT_JOBS=2
PROCESSING_INPUT_DIR=/app/data/inputs
PROCESSING_OUTPUT_DIR=/app/data/outputs
PROCESSING_TEMP_DIR=/app/data/temp

# External API Keys
PIXABAY_API_KEY=your-pixabay-api-key-here

# AWS Configuration (set these when deploying)
AWS_REGION=ap-southeast-2
AWS_ACCOUNT_ID=your-aws-account-id

# Application Configuration
NODE_ENV=production
PORT=8000

# Database Connection
DB_HOST=database
DB_PORT=3306

# Security Notes:
# 1. Never commit this file with real values to version control
# 2. Use AWS Secrets Manager or similar for sensitive values in production
# 3. Ensure JWT_SECRET is at least 32 characters long and random
# 4. Use strong passwords for database access
# 5. Regularly rotate secrets and passwords